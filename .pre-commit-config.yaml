repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.16
    hooks:
      # Run the linter (with autofix).
      - id: ruff-check
        args: [--fix]
      # Run the formatter.
      - id: ruff-format
